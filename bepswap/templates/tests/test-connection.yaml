apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "bepswap.fullname" . }}-test-connection"
  labels:
{{ include "bepswap.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "bepswap.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
