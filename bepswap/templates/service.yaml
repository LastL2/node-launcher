apiVersion: v1
kind: Service
metadata:
  name: {{ include "bepswap.fullname" . }}
  labels:
    {{- include "bepswap.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "bepswap.selectorLabels" . | nindent 4 }}
