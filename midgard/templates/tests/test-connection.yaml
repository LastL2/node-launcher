apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "midgard.fullname" . }}-test-connection"
  labels:
{{ include "midgard.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "midgard.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
