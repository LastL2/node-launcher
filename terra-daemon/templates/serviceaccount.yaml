{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "terra-daemon.serviceAccountName" . }}
  labels:
{{ include "terra-daemon.labels" . | nindent 4 }}
{{- end -}}
