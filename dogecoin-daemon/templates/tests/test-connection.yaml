apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "dogecoin-daemon.fullname" . }}-test-connection"
  labels:
{{ include "dogecoin-daemon.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "dogecoin-daemon.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
