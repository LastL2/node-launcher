# Default values for thornode.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

net: mainnet
type: validator

signer:
  name: thorchain
  password: password
  mnemonicSecret: ""
  passwordSecret: ""

peer:
seeds:
peerApi:
binanceDaemon:
ethRouterContract:
  testnet: "0x9d496De78837f5a2bA64Cb40E62c19FBcB67f55a"
  mainnet: "0xAa749190f7259cdd2a18cb0Ac42957dA4d8909e0"

enableApi: true

debug: false

haltHeight:

replicaCount: 1

hostNetwork: false

strategyType: Recreate

image:
  repository: registry.gitlab.com/thorchain/thornode
  pullPolicy: IfNotPresent
  tag: mainnet

imagePullSecrets: []
nameOverride: ""
fullnameOverride: ""

service:
  type: ClusterIP
  port:
    api: 1317
    testnet:
      p2p: 26656
      rpc: 26657
    mainnet:
      p2p: 27146
      rpc: 27147

persistence:
  enabled: true
  size: 300Gi
  accessMode: ReadWriteOnce
  # existingClaim: your-pvc
  # storageClass: "-"
  # hostPath: /host-path
  # subPath: subpath

serviceAccount:
  # Specifies whether a service account should be created
  create: true
  # The name of the service account to use.
  # If not set and create is true, a name is generated using the fullname template
  name:

priorityClassName: ""

podSecurityContext: {}
  # fsGroup: 2000

securityContext: {}
  # capabilities:
  #   drop:
  #   - ALL
  # readOnlyRootFilesystem: true
  # runAsNonRoot: true
  # runAsUser: 1000

resources:
  requests:
    cpu: 1
    memory: 2Gi
  limits:
    # cpu: 2
    memory: 4Gi

nodeSelector: {}

tolerations: []

affinity: {}

global: {}
