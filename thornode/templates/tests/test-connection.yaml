apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "thornode.fullname" . }}-test-connection"
  labels:
{{ include "thornode.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "thornode.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
