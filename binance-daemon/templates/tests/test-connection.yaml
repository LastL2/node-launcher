apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "binance-daemon.fullname" . }}-test-connection"
  labels:
{{ include "binance-daemon.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "binance-daemon.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
