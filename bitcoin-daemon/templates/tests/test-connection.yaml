apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "bitcoin-daemon.fullname" . }}-test-connection"
  labels:
{{ include "bitcoin-daemon.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "bitcoin-daemon.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
