apiVersion: v1
kind: Service
metadata:
  name: {{ include "thor-api.fullname" . }}
  labels:
    {{- include "thor-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "thor-api.selectorLabels" . | nindent 4 }}
