apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "litecoin-daemon.fullname" . }}-test-connection"
  labels:
{{ include "litecoin-daemon.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "litecoin-daemon.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
