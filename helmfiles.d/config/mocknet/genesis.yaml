global:
  tag: mocknet
  net: mocknet
  mnemonicSecret: thornode-mnemonic
  gateway:
    enabled: false

thor-gateway:
  enabled: false

thor-daemon:
  service:
    type: ClusterIP

bifrost:
  binanceStartBlockHeight: 1
  service:
    type: ClusterIP

midgard:
  image:
    tag: mocknet
